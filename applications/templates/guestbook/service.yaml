---
apiVersion: v1
kind: Service
metadata:
  name: guestbook-routing
  annotations:
    getambassador.io/config: |
      ---
      apiVersion: ambassador/v1
      kind:  Mapping
      name:  guestbook_mapping
      prefix: /guestbook/
      service: guestbook-ui
spec:
  ports:
    - port: 80
